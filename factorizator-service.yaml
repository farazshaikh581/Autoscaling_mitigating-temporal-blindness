apiVersion: v1
kind: Service
metadata:
  name: factorizator
  namespace: factorizator
spec:
  type: NodePort
  selector:
    app: factorizator
  ports:
  - name: http
    port: port
    targetPort: targetport
    nodePort: nodeport
